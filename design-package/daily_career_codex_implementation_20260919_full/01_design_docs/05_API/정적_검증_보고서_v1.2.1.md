# OpenAPI v1.2.1 정적 검증 보고서

**실행일:** 2026-09-19  
**결과:** **PASS**

## 검증 결과

| 검사 | 결과 |
|---|---:|
| 전체 인터페이스 | 91 |
| JSON 인터페이스 | 88 |
| OAuth 리다이렉트 | 3 |
| 스키마 정의 | 217 |
| 로컬 `$ref` | 1,036 |
| Request 예시 | 24 |
| 정상 Response 예시 | 88 |
| 오류 Response 예시 | 633 |
| 음성 스키마 사례 | 14 |
| 의미/산식 정합성 검사 | 59 |
| 오류 | 0 |

## P0 전용 검사

- `completionRate` 사용 스키마가 `Curriculum`, `OverallProgress`로 한정됨
- 전체 커리큘럼 스키마에 `progressRate` 잔존 없음
- `OverallProgress`, `PeriodProgress`, `Evaluation`에 `adherenceRate` 존재
- `OverallProgress`, `PeriodProgress`, `CategoryProgress`, `Evaluation`에 `masteryRate` 존재
- `masteryRate`와 `firstAttemptAccuracy` 설명/필드 분리 확인
- `PeriodProgress`, `Evaluation` 내부 완료율은 `progressRate` 사용
- 전체 `completionRate` 산식 예시 검증
- 기간 `progressRate` 산식 예시 검증
- `firstAttemptAccuracy` 산식 예시 검증
- 인터페이스 수 91개 유지
- `/cancel` 경로 없음
- 공개 OpenAPI에 `CANCELED` 상태 없음

## 공개 필드 분포

| 필드 | 스키마 |
|---|---|
| `completionRate` | `Curriculum`, `OverallProgress` |
| `adherenceRate` | `OverallProgress`, `PeriodProgress`, `Evaluation` |
| `masteryRate` | `OverallProgress`, `PeriodProgress`, `CategoryProgress`, `Evaluation` |
| `firstAttemptAccuracy` | `OverallProgress`, `DailyProgress`, `PeriodProgress`, `CategoryProgress`, `Evaluation` |
| `progressRate` | `CurriculumMonth`, `WeekSummary`, `LearningDay`, `WeekDetail`, `DayProgress`, `DailyProgress`, `PeriodProgress`, `CategoryProgress`, `CalendarDay`, `Evaluation` |

## 검증하지 않은 범위

- 실제 Spring Backend 실행
- 실제 DB/트랜잭션/동시성 테스트
- 실제 Frontend TypeScript 컴파일
- Google/Notion/Slack/Web Push 외부 연동
- 실제 mastery/adherence 계산 구현

`masteryRate`의 구체 가중치와 문제/시험 결합 산식은 기존 확정 자료에 없으므로 임의로 확정하지 않는다.
